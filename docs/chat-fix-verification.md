# 本次改动 — 功能检验清单

> 本文档针对 **本次修改** 涉及的功能进行检验。
> ⚡ = AI 已自动验证通过 | 🖐️ = 需要手动验证

---

## 一、已修改文件清单

| 文件 | 改动 |
|------|------|
| `index.html` | viewport 添加 `viewport-fit=cover` |
| `MarketplaceContext.jsx` | `otherUserId` 字段 + `findConversation` + `createConversationWithMessage`（含卖家昵称查询、自发消息拦截） |
| `ChatRoom.jsx` | CSS `dvh` 修复 + safe-area padding + 对方名字可点击 + 新会话延迟创建 |
| `ProductDetail.jsx` | 改用 `findConversation`，无已有会话时导航到 `/chat/new` |
| `UserProfile.jsx` | 新建用户公开主页 |
| `main.jsx` | 添加 `/user/:id` 路由 |
| `patch_profiles_public_read.sql` | profiles 表公开可读的 SQL 补丁 |

---

## 二、问题1：聊天输入框安卓偏低

| # | 检查项 | 状态 |
|---|--------|------|
| 1 | `index.html` 包含 `viewport-fit=cover` | ⚡ 已确认 |
| 2 | ChatRoom 容器使用 `h-screen h-[100dvh]`（dvh 优先，vh 兜底） | ⚡ 已确认 |
| 3 | 输入框使用 `style={{ paddingBottom: 'max(1rem, env(safe-area-inset-bottom, 0.5rem))' }}` | ⚡ 已确认 |
| 4 | 安卓设备上输入框和发送按钮不被遮挡 | 🖐️ **需朋友在安卓手机上打开聊天页面确认** |
| 5 | iOS 设备上输入框位置正常（不影响原来的行为） | 🖐️ **需在 iPhone 上确认** |

---

## 三、问题2：查看对方主页

| # | 检查项 | 状态 |
|---|--------|------|
| 1 | `/user/:id` 路由已注册，页面可访问 | ⚡ 已确认 |
| 2 | 无效用户 ID → 显示"用户不存在"提示 | ⚡ 已确认 |
| 3 | 页面显示头像、昵称、学校、认证状态、用户 ID | 🖐️ **需使用真实用户 ID 访问确认** |
| 4 | 页面显示该用户发布的商品列表 | 🖐️ **需使用有发布过商品的用户 ID 确认** |
| 5 | 点击商品卡片可跳转到商品详情 | 🖐️ **同上** |
| 6 | 聊天室 header 中对方名字可点击 → 跳转到 `/user/:id` | 🖐️ **需在真实聊天会话中点击对方名字确认** |

> ⚠️ **需要你手动执行的 SQL**：为了让未登录用户也能查看用户主页，请在 Supabase Dashboard → SQL Editor 中运行 `docs/patch_profiles_public_read.sql`。如果你只希望已登录用户才能查看，可以不运行此 SQL。

---

## 四、问题3：未发消息不创建会话

| # | 检查项 | 状态 |
|---|--------|------|
| 1 | 商品详情页点击"私聊" → 先查找已有会话 | ⚡ 代码确认 |
| 2 | 无已有会话 → 导航到 `/chat/new?productId=xxx`（不插入 DB） | ⚡ 浏览器确认 |
| 3 | 聊天室显示商品信息栏和"发送第一条消息开始对话"提示 | ⚡ 浏览器确认 |
| 4 | 输入消息 → 点击发送 → 才创建DB会话和消息记录 | 🖐️ **需登录后实际发送消息确认** |
| 5 | 发送后 URL 从 `/chat/new?...` 变为 `/chat/真实ID` | 🖐️ **同上** |
| 6 | 不发消息直接返回 → 对方看不到任何新会话 | 🖐️ **需两个账号验证：A 进入新聊天不发消息就退出 → B 的 Inbox 无新会话** |
| 7 | 已有会话时点击"私聊" → 直接进入已有会话（不创建新的） | 🖐️ **需使用已存在的聊天对话确认** |

---

## 五、自检修复项

| # | 检查项 | 状态 |
|---|--------|------|
| 1 | 创建会话时 `seller_name` 使用真实昵称（查 profiles 表） | ⚡ 代码确认 |
| 2 | 自己的商品不能给自己发消息 → Toast "不能给自己的商品发消息" | 🖐️ **可手动构造 URL `/chat/new?productId=自己的商品ID` 测试** |
| 3 | `conversations` 数据包含 `otherUserId` 字段 | ⚡ 代码确认 |

---

## 六、不应受影响的功能（回归测试）

| # | 检查项 | 状态 |
|---|--------|------|
| 1 | 首页加载商品 | ⚡ 已确认 |
| 2 | 商品详情页显示正常 | ⚡ 已确认 |
| 3 | 404 页面正常 | ⚡ 已确认 |
| 4 | 底部导航栏在聊天页隐藏 | ⚡ 已确认（hidePaths 覆盖 `/chat`） |
| 5 | 语言切换正常 | ⚡ 已确认 |
| 6 | 登录/注册流程 | 🖐️ **建议快速走一遍登录流程** |
| 7 | 发布/编辑商品 | 🖐️ **建议快速走一遍发布流程** |
| 8 | Inbox 会话列表显示 | 🖐️ **登录后确认 Inbox 正常** |
| 9 | 已有聊天室的消息收发 | 🖐️ **打开已有会话确认消息正常显示和发送** |
| 10 | 收藏功能 | 🖐️ **点击心形确认收藏正常** |
| 11 | 举报弹窗 | 🖐️ **商品详情页点击举报图标确认弹窗正常** |
| 12 | 个人中心编辑和认证 | 🖐️ **确认 Profile 页面正常** |

---

## 七、汇总

| 类别 | ⚡ AI 已验证 | 🖐️ 需手动验证 |
|------|-------------|---------------|
| 问题1：安卓 CSS | 3 项 | 2 项 |
| 问题2：对方主页 | 2 项 | 4 项 |
| 问题3：延迟创建 | 3 项 | 4 项 |
| 自检修复 | 2 项 | 1 项 |
| 回归测试 | 5 项 | 7 项 |
| **合计** | **15 项** | **18 项** |

---

## 八、你需要做的事情

1. **运行 SQL 补丁**（可选）：`docs/patch_profiles_public_read.sql` → Supabase Dashboard → SQL Editor
2. **安卓手机测试**：让朋友打开聊天页面，确认输入框位置正常
3. **测试延迟创建**：
   - 登录后点击某商品"私聊" → 进入空聊天 → **不发消息退出** → 确认对方看不到新会话
   - 再次进入 → **发一条消息** → 确认会话创建成功
4. **测试用户主页**：在聊天室点击对方名字 → 确认跳转到对方主页
5. **快速回归测试**：登录/发布/编辑/收藏/举报各走一遍
